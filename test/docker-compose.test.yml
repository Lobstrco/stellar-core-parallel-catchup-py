version: '2.1'

services:
  db:
    image: postgres
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: test

  stellar-core:
    environment:
      DB_USER: test
      DB_PASSWORD: test
      DB_HOST: db
      NODE_SECRET_KEY: SCJXQMNKNZBOEIKJML6GD7XZJADW3YYFQZRZ6IPU2PPNPX33NESKLULC
    build:
      context: ..
      dockerfile: ./test/Dockerfile
      args:
        core_version: 12.2.0-29
    command: bash -c "sleep 10 && /tmp/wait-for-it.sh db:5432 && . /tmp/runtests.sh"
